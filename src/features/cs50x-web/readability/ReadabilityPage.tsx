import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function ReadabilityPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Readability"
      slug="readability"
      description="Build the text input UI and readability-calculation logic inside this feature folder."
      componentFile="ReadabilityPage.tsx"
    />
  );
}
