import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function DnaPage() {
  return (
    <Cs50xProjectPlaceholder
      title="DNA"
      slug="dna"
      description="Build the STR matching logic, sequence helpers, and comparison UI inside this feature folder."
      componentFile="DnaPage.tsx"
    />
  );
}
