import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function CaesarPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Caesar"
      slug="caesar"
      description="Build the Caesar cipher form, validation, and encryption logic inside this feature folder."
      componentFile="CaesarPage.tsx"
    />
  );
}
