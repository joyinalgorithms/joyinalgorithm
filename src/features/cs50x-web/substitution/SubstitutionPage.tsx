import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function SubstitutionPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Substitution"
      slug="substitution"
      description="Build the substitution key validation, cipher helpers, and UI inside this feature folder."
      componentFile="SubstitutionPage.tsx"
    />
  );
}
