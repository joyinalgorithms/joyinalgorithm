import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function SpellerPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Speller"
      slug="speller"
      description="Build the dictionary lookup logic, parsing helpers, and results UI inside this feature folder."
      componentFile="SpellerPage.tsx"
    />
  );
}
