import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function FilterPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Filter"
      slug="filter"
      description="Build the image processing UI and filter helpers inside this feature folder."
      componentFile="FilterPage.tsx"
    />
  );
}
