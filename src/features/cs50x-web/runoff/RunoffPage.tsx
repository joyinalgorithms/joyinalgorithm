import Cs50xProjectPlaceholder from "../shared/Cs50xProjectPlaceholder";

export default function RunoffPage() {
  return (
    <Cs50xProjectPlaceholder
      title="Runoff"
      slug="runoff"
      description="Build the ranked-choice rounds, elimination logic, and results UI inside this feature folder."
      componentFile="RunoffPage.tsx"
    />
  );
}
